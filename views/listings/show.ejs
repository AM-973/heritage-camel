<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>
        <%= foundListing.title %>
    </title>
</head>

<body>
    
        <nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navmarketplace navbar-brand text-primary " href="/"><img src="/images/camel.jpg"" alt="Heritage camel" class="img-fluid" style="width: 50px; height: 50px; border-radius: 50%;"/></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if (user) { %>
                    <li class="nav-item"><a class="nav-link" href="/auth/sign-out">Sign Out</a></li>
                    <% } else { %>
                    <li class="nav-item"><a class="nav-link" href="/auth/sign-up">Sign Up</a></li>
                    <li class="nav-item"><a class="nav-link" href="/auth/sign-in">Sign In</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container my-5 listing-details"> 
        <h2>
        <strong>Listing name:</strong> <%= foundListing.title %>
        </h2>
        <p><strong>By:</strong> <%= foundListing.User.username %></p>
        <img src="<%= foundListing.image.url %>" class="listing-img" alt="Image of <%= foundListing.title %>" style="border-radius: 10px;"/>
        <main style="display:flex; flex-direction: column; align-items: center; justify-content: center; margin-top: 20px;">
        <div class="card" style="width: 25rem;">
        <p> <strong>Description:</strong> <%= foundListing.description %></p>
        <p> <strong>Location:</strong> <%= foundListing.location ? foundListing.location : 'Not specified' %> </p>
        <p> <strong>Date visited:</strong> <%= foundListing.date ? new Date(foundListing.date).toDateString() : 'Not specified' %></p> 
        <p> <strong>Notes:</strong> <%= foundListing.notes %> </p>
        </div>
        </main>
    </div>
    <% if (user && foundListing.User._id.equals(user._id)) { %>
        <form action="/listings/<%= foundListing._id %>?_method=DELETE" method="POST" class="text-center">
            <a href="/listings/<%= foundListing._id %>/edit" class="btn btn-primary">Edit Listing</a>
            <a href="/listings" class="btn btn-secondary">Back to Listings</a>
            <br><br>
            <button type="submit" id="delete-btn" style="margin:0px;">Delete</button>
        </form>
        <% } %>

        <h3 style="justify-content: center;text-align: center;">Leave a Comment</h3>
    <form action="/listings/<%= foundListing._id %>/comments" method="POST">
        <textarea name="content" id="" required></textarea>
        <button type="submit">Post Comment</button>
    </form>
    <h3 class="mt-5" style="justify-content: center;text-align: center;">Comments</h3>
    <% if (foundListing.comments.length === 0) { %>
        <p class="text-center">No comments yet. Be the first to comment!</p>
    <% } else { %>
    <ul>
        <% foundListing.comments.forEach((comment) => { %>
            <li class="list-group-item" style="justify-content: center;text-align: center;"> 
                <%= comment.content %> - <%= comment.author.username %> 
                <% if (user && comment.author._id.equals(user._id)) { %>
                    <form action="/listings/<%= foundListing._id %>/comments/<%= comment._id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                <% } %>
            </li>
        <% }) %>
        <% } %>
    </ul>
   
</body>
        
</html>